- [ ] For tiled images, calculate real X/Y values of pixels in region of interest
- [ ] Ignore result folder
- [ ] Describe file extensions
- [ ] Describe templates

- [ ] Handle non ASCII chars when inserting to DB
- [ ] Support the filtering (validity) for image plane and cluster
- [ ] Rethink control image naming to keep the order and store the ref to a control image in the database

(gdb) handle SIGSEGV nostop noprint pass

handle SIGSEGV nostop print pass
readelf -d imagec


Linux:

sudo apt install libxcb-cursor-dev
sudo apt install libxcb-*



## Liuc runs.sh

appname=`basename $0 | sed s,\.sh$,,`

dirname=`dirname $0`
tmp="${dirname#?}"

if [ "${dirname%$tmp}" != "/" ]; then
dirname=$PWD/$dirname
fi
LD_LIBRARY_PATH=$dirname
QT_PLUGIN_PATH=$dirname/plugins
FONTCONFIG_PATH=/etc/fonts/
export QT_QPA_PLATFORM=xcb
#export QT_DEBUG_PLUGINS=1
export LD_LIBRARY_PATH
export QT_PLUGIN_PATH
export FONTCONFIG_PATH

$dirname/$appname "$@"





          key: conan-cache-${{ runner.os }}-${{ hashFiles('conanfile.py') }}
