{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "blur_settings.schema.json",
    "title": "Blur",
    "description": "Image blurring in image processing is a technique that reduces detail and sharpness, making an image appear smoother or softer, achieved by applying a convolution kernel (a small matrix filter like Gaussian or Box) to average pixel values, effectively removing high-frequency noise and creating smooth transitions, used for noise reduction, artistic effects, or privacy.",
    "type": "object",
    "properties": {
        "mode": {
            "type": "string",
            "enum": [
                "Blur",
                "GaussianBlur"
            ]
        },
        "kernelSize": {
            "type": "integer",
            "default": 3,
            "minimum": 1,
            "not": {
                "multipleOf": 2
            }
        },
        "repeat": {
            "type": "integer",
            "default": 1,
            "minimum": 1
        }
    },
    "required": [
        "mode",
        "kernelSize",
        "repeat"
    ]
}
